# Intro to React Router DOM

## Part I: useParams & useNavigate

- what/why `useParams` and how does it work?
  - creating the `PokemonDetailPage.jsx`
  - displaying a pokemon upon mounting
- what/why `useNavigate`?
  - `Link` vs `useNavigate`
  - adding the `view details` button to the `PokemonCard`


## Part II: useOutletContext

- what problem does `useOutletContext` solve?
  - what is prop drilling?
- How to utilize `useOutletContext`
  - parent => `App.jsx` to child => `pages` relationship
  - refactoring to maintain state
    - `pokemonData` at the `App.jsx` Component.